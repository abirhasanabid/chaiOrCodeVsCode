// Check if a number is positive or negative using a ternary operator.
const number = -120;
const positive = number < 0 ? 'Negative' : 'positive';
console.log(positive);


// Check if a person is eligible to vote (age 18 or more).

const age = 12;
const forVote = age >= 18 ? "eligible" : "not eligible";
console.log(forVote);


// If a user is logged in, show "Dashboard"; otherwise, show "Login Page".

const isLoggedIn = true;
const login = isLoggedIn ? "Dashbord" : "Login Page";
console.log(login);
